# Integration test coverage — rewrite tracker
> Source: root tests/*.rs. 185 items. Check off what the rewrite re-covers.

- [ ] First apply deploys a single-file portal as a symlink to the source file with a recorded entry.
- [ ] First apply deploys a symlinked source file as a link to its logical portal path, not its resolved target.
- [ ] First apply with a copy rule resolves a symlinked source into a regular file with content.
- [ ] First apply deploys a literal directory portal including all nested files.
- [ ] First apply maps children of a symlinked directory portal under their logical source paths.
- [ ] First apply with a copy rule deploys a regular file and records its content hash.
- [ ] First apply with a template rule renders data-file variables into the target.
- [ ] First apply with a mode rule sets the configured permissions on the deployed copy.
- [ ] First apply resolves a templated portal source key from a data-file variable.
- [ ] First apply of an empty directory portal succeeds while deploying nothing and recording nothing.
- [ ] Fresh apply creates a missing target root and populates it.
- [ ] Dry-run of a fresh deployment changes nothing on disk and records nothing.
- [ ] Re-apply rewires the symlink when the source file content changes, without prompting.
- [ ] Re-apply redeploys a copy and updates its recorded hash when the source changes.
- [ ] Re-apply re-renders a template when a data-file variable changes.
- [ ] Re-apply converts an existing symlink into a plain file when a copy rule is added.
- [ ] Re-apply reverts an existing copy back to a symlink when the copy rule is removed.
- [ ] Re-apply updates permissions on an existing copy when the mode rule changes.
- [ ] Re-apply redirects the record to the new path when the source file is renamed.
- [ ] Activating a profile between runs re-renders templates with the profile override.
- [ ] Skipping an unmanaged target keeps the existing file, records nothing, and reports skipped.
- [ ] Replacing an unmanaged target deploys the desired entry and records it.
- [ ] Replace-all latches across multiple unmanaged obstructions without re-prompting.
- [ ] Cancelling at a prompt stops the run while preserving already completed entries.
- [ ] Skipping a tampered managed copy retains the tampered content and the old record.
- [ ] Replacing a tampered managed copy restores the source content and hash.
- [ ] Replacing a tampered managed symlink restores the link and its recorded kind.
- [ ] Skipping a parent-path obstruction leaves the blocking file intact and deploys nothing there.
- [ ] Replacing a parent-path obstruction creates directories and deploys the nested entry.
- [ ] Deployment traverses a directory-symlink parent without prompting.
- [ ] A dangling parent symlink is replaced by only the link itself, leaving the external file safe.
- [ ] Clean-up removes a stale symlink and drops its record.
- [ ] Clean-up removes a stale copy and drops its record.
- [ ] Clean-up removes a stale template and drops its record.
- [ ] Clean-up removes every file deployed under a removed directory portal.
- [ ] Clean-up keeps still-desired targets while removing stale ones.
- [ ] Clean-up is a no-op when nothing is stale.
- [ ] Clean-up relinquishes a tampered stale path, leaving the file but dropping the record.
- [ ] Clean-up relinquishes a missing stale target by dropping its record.
- [ ] Clean-up relinquishes child records untouched when a deployed directory was replaced by a file.
- [ ] Clean-up relinquishes rather than deletes when a deployed file was replaced by a directory.
- [ ] Clean-up with prune removes the emptied parent chain up to the target root.
- [ ] Without prune, emptied parent directories remain on disk.
- [ ] Prune stops at a parent that still holds a desired target.
- [ ] Prune stops at a directory holding an unmanaged subdirectory.
- [ ] Prune stops at a symlink parent, leaving the link and its target intact.
- [ ] Clean-up unlinks a stale managed file reached behind a symlink parent without touching the link itself.
- [ ] A skipped entry blocks clean-up of other stale entries for that run.
- [ ] Records pointing outside the current target root are never clean-up candidates.
- [ ] Dry run leaves an obstruction unprompted and untouched.
- [ ] Dry run leaves a tampered managed target untouched with its old record.
- [ ] Dry run with clean-up keeps stale targets and their records.
- [ ] Dry run with clean-up and prune keeps stale directories.
- [ ] An empty variable key fails before deploying anything.
- [ ] A missing literal portal source fails before deploying anything.
- [ ] A template render failure mid-run preserves already completed entries.
- [ ] Deleting the source between runs fails the preflight check.
- [ ] A dangling symlink as target root is rejected before any deployment.
- [ ] A dangling symlink as target root is rejected even for an empty deployment.
- [ ] Config reading assembles the desired deployment, entries, and variable context from config and data files.
- [ ] A symlink effective type drops an earlier wildcard mode with a warning.
- [ ] An active profile overrides a base variable in both rendered config paths and the variable context.
- [ ] An active profile without a definition is ignored, keeping base variables.
- [ ] A missing template variable in config fails before TOML parsing.
- [ ] Rendered config values are validated like literal values.
- [ ] An unreadable `.dotriftignore` directory fails as a configuration error.
- [ ] A malformed data file names its path in the error chain.
- [ ] A float variable value is rejected as a configuration error.
- [ ] An empty variable key is rejected as a configuration error.
- [ ] An empty profile binding key is rejected as a configuration error.
- [ ] An empty profile name is rejected as a configuration error.
- [ ] A missing `dotrift.toml` names its path in the error chain.
- [ ] Malformed TOML after rendering names the config file in the error chain.
- [ ] An unknown root key is rejected.
- [ ] A rule key with a double slash is rejected as an invalid rule path.
- [ ] A rule key with a trailing slash is rejected as an invalid rule path.
- [ ] A literal file portal targeting the root destination is rejected.
- [ ] A glob root destination colliding with a literal target is rejected.
- [ ] A `./`-prefixed portal value colliding with a plain target is rejected.
- [ ] A glob root destination structurally conflicting with an ancestor target is rejected.
- [ ] Collisions across portals report the collision location.
- [ ] A relative target directory is rejected as non-absolute.
- [ ] An explicit target override beats the configured target directory.
- [ ] The configured target directory is used when no override is given.
- [ ] Home directory is used as fallback when neither override nor configured target exists.
- [ ] `.dotriftignore` excludes entries while control files never deploy.
- [ ] A negation pattern re-includes a target ignored by an earlier pattern.
- [ ] A negated pattern can re-include a control file.
- [ ] A nested control filename deploys normally when mapped explicitly.
- [ ] Ignoring colliding targets prevents collision validation.
- [ ] A directory-only ignore pattern is inert for file mapping.
- [ ] A profile overlay replaces the whole variable value without recursive merging.
- [ ] A missing source directory is rejected.
- [ ] A source path that is a file is rejected as not a directory.
- [ ] A target inside the source directory is rejected as overlapping.
- [ ] A missing global config file leaves apply behavior unchanged.
- [ ] Malformed global config TOML fails the run before any change.
- [ ] Malformed global config TOML fails a dry run before any change.
- [ ] An unknown root section in global config fails the run before any change.
- [ ] An unknown root key in global config fails the run before any change.
- [ ] An unknown pager property in global config fails the run before any change.
- [ ] An unknown apply property in global config fails the run before any change.
- [ ] Pager args as a non-array string are rejected before any change.
- [ ] A non-string pager arg is rejected before any change.
- [ ] A non-boolean `replace-identical` value is rejected before any change.
- [ ] A non-string pager command is rejected before any change.
- [ ] Pager args without a command are rejected before any change.
- [ ] A non-table pager value is rejected before any change.
- [ ] An empty pager table is rejected before any change.
- [ ] A config with both pager and apply sections is accepted.
- [ ] An empty apply table is accepted.
- [ ] An explicit default apply value is accepted.
- [ ] An empty pager command is accepted.
- [ ] A whitespace pager command is accepted.
- [ ] A config path that is a directory fails the run.
- [ ] A dangling symlink at the config path counts as missing and does not fail.
- [ ] Global config is read before control files, so config errors surface first.
- [ ] Profile list reports nothing when no data file exists.
- [ ] Profile list reports nothing when no profiles are defined.
- [ ] Profile list annotates the active profile in sorted output.
- [ ] Profile list annotates every active profile.
- [ ] Profile list omits an active profile that has no definition.
- [ ] Profile show reports nothing when no data file exists.
- [ ] Profile show resolves variable values through the active profile override.
- [ ] Profile show ignores an active profile that has no definition.
- [ ] Profile list with a nonexistent source directory errors.
- [ ] Activating a defined profile confirms and records activation.
- [ ] Activating an undefined profile errors and activates nothing.
- [ ] Re-activating a profile moves it to the end of precedence order.
- [ ] Profile list without a source directory errors.
- [ ] Profile activate without a source directory errors.
- [ ] Profile show without a source directory errors.
- [ ] Deactivating an active profile confirms and clears it.
- [ ] Deactivating an inactive profile errors.
- [ ] Deactivating a stale profile clears it even without a definition.
- [ ] Profile show formats all base variable types.
- [ ] Profile show prioritizes the most recently activated profile.
- [ ] Profile show unions variables across all active profiles.
- [ ] Activate then deactivate round-trips show output back to base values.
- [ ] View-diff prompt shows a single-line copy diff.
- [ ] View-diff prompt shows a rendered template diff.
- [ ] A blank `DOTRIFT_PAGER` falls back to `PAGER`.
- [ ] A whitespace `DOTRIFT_PAGER` falls back to `PAGER`.
- [ ] With no pager configured, the diff prints to stdout.
- [ ] A failing `PAGER` binary falls back to stdout.
- [ ] A failing `DOTRIFT_PAGER` binary raises an error.
- [ ] The configured pager is used when `DOTRIFT_PAGER` is unset.
- [ ] `DOTRIFT_PAGER` overrides the configured pager.
- [ ] An empty configured pager command falls through to `PAGER`.
- [ ] Configured pager args are passed literally, preserving spaces.
- [ ] A failing configured pager fails the run without falling back.
- [ ] Fresh verbose output prints per-path lines plus a summary.
- [ ] Fresh default output prints only the summary.
- [ ] Clean-up verbose output reports removed entries plus a summary.
- [ ] Clean-up default output reports only the summary.
- [ ] Prune verbose output reports pruned parent directories.
- [ ] Prune default output reports only the summary.
- [ ] Fresh quiet mode suppresses all output.
- [ ] Clean-up quiet mode suppresses all output.
- [ ] Prune quiet mode suppresses all output.
- [ ] An identical symlink obstruction is replaced without prompting when `replace-identical` is set.
- [ ] Identical bytes with a different mode are replaced and the rule mode is reapplied without prompting.
- [ ] A divergent copy obstruction still prompts even with `replace-identical` set.
- [ ] An identical template obstruction is replaced without prompting.
- [ ] A parent obstruction still prompts even with `replace-identical` set.
- [ ] A replace-all latch subsumes the identical check for later entries.
- [ ] Dry run reports an identical obstruction as replaced without changing it.
- [ ] Dry run reports a template obstruction without rendering it.
- [ ] A multi-target template deploys the same render to every target and cleans the registry.
- [ ] A render failure leaves the target absent and the registry emptied.
- [ ] A stale registry entry from a killed run is discarded and re-rendered.
- [ ] A dry run creates nothing in the render registry.
- [ ] A dry run leaves a preexisting registry untouched.
- [ ] View-diff fails when the render registry is unavailable while leaving the target intact.
- [ ] Template deploy falls back to direct rendering when the registry is unavailable.
- [ ] View-diff then replace deploys the shared render and cleans the registry.
- [ ] Property test deploys an exact symlink tree matching the generated world.
- [ ] Property test clean-up drops removed targets to match the pruned world.
- [ ] Property test clean-up with prune leaves the target directory empty.
- [ ] Property test dry-run output matches what a real deploy would create, without creating it.
- [ ] Property test dry-run clean-up output matches the removals a real clean-up performs.
- [ ] Status reports nothing when no database exists.
- [ ] Status reports nothing for an empty database.
- [ ] Status prints sorted lines with verdicts for managed, changed, and missing entries.
- [ ] Status layout is unchanged when color output is forced.

[package]
name = "demo"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"

[[bin]]
name = "prompt"
path = "src/bin/prompt.rs"

[dependencies]
dotrift = { path = ".." }
tempfile.workspace = true

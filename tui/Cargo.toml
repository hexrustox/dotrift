[package]
name = "tui"
version = "0.1.0"
edition = "2024"

[dependencies]
crossterm = { version = "0.29.0", default-features = false, features = ["events"] }
strum = { version = "0.28", features = ["derive"] }
